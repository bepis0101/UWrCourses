<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Deklaracja</title>
</head>
<body>
    <form method="post" action="/print">
        <div>
            <% if ( errors ) { %>
                <p><%= errors.error %></p>
            <% } %>
            <label>
                Name:
                <input type="text" name="name" placeholder="wymagane">
            </label>
        </div>
        <div>
            <label>
                Surname:
                <input type="text" name="surname" placeholder="wymagane">
            </label>
        </div>
        <div>
            <label>
                Subject:
                <input type="text" name="subject" placeholder="wymagane">
            </label>
        </div>
        <% for ( var i=0; i<10; i++ ) { %>
        <div>
            <label>
                Zadanie <%= i+1 %>:
                <input type="number" min="0" name="tasks[]" value="0">
            </label>
        </div>
        <% } %>
        <button type="submit">Submit</button>
    </form>
</body>
</html>